# cannot load environment using RSuite because not global instance (if exists)
#  is required
.libPaths(c('../../deployment/sbox', '../../deployment/libs', .libPaths()))

if ("roxygen2" %in% installed.packages()[, "Package"]) {
  library(roxygen2)
}
